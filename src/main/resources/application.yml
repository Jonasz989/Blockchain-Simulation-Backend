spring:
    datasource:
        url: jdbc:postgresql://postgres.cnow9afpsle0.us-east-1.rds.amazonaws.com:5432/postgres
        username: postgres
        password: postgres
        name: postgres
    sql:
        init:
            mode: always
    jpa:
        properties:
            hibernate:
                dialect:
                    org.hibernate.dialect.PostgreSQLDialect

        hibernate:
            ddl-auto: create-drop
#    flyway:
#        baseline-on-migrate: true
